spring:
  sql:
    init:
      #     mode: always
      mode: never
  jpa:
    hibernate:
      ddl-auto: create

cloudinary:
  api-key: 446412117829948
  api-secret: ${CLOUDINARY_API_SECRET}
  cloud-name: drapmo8cx